<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/send"
        android:title="@string/send_label"
        android:icon="@drawable/ic_send_white"
        app:showAsAction="always"
        android:visible="false"/>

    <item android:id="@+id/edit"
          android:title="@string/edit_label"
        android:visible="false"/>

    <item android:id="@+id/start"
          android:title="@string/in_progress_action_label"
          android:visible="false"/>

    <item android:id="@+id/complete"
          android:title="@string/complete_label"
          android:visible="false"/>
</menu>